[module/date]
type = internal/date

interval = 1.0

time = "%a %b %d    %I:%M %p"
;time-alt = %B %d, %Y

format = <label>
label = %time%
; custom setup:
; type = internal/date
; interval = 3

; date =
; date-alt = "%YYYY-%mm-%dd"

; time = %H:%M
; ; time-alt = %H:%M:%S
; time-alt = "%d-%m-%Y"
; label-padding = 1
; format = <label>
; ; format-prefix = 
; format-prefix-foreground = ${colors.module_bg}
; format-underline = #0a6cf5
; label = %time%
