input type:keyboard {
    repeat_delay 250
    repeat_rate 60
    xkb_options caps:escape
}

input type:TouchPad {
    accel_profile adaptive 
    pointer_accel 1
}

input type:Mouse {
  accel_profile flat
  pointer_accel 0
}
